# 카카오톡 공유 기능 설정 가이드

## 1. 카카오 개발자 계정 및 앱 생성

### 1.1 카카오 개발자 센터 접속
1. [카카오 개발자 센터](https://developers.kakao.com) 접속
2. 카카오 계정으로 로그인

### 1.2 애플리케이션 추가
1. **내 애플리케이션** 메뉴 클릭
2. **애플리케이션 추가하기** 버튼 클릭
3. 앱 정보 입력:
   - **앱 이름**: `기후시민 MBTI` (또는 원하는 이름)
   - **사업자명**: 본인 이름 또는 단체명
   - **카테고리**: 라이프스타일 > 환경/사회/정치
4. **저장** 클릭

## 2. 앱 키 확인

1. 생성한 앱 선택
2. **앱 키** 섹션에서 **JavaScript 키** 복사
   ```
   예시: 97ac98a9d0c4772eda33283870671745
   ```

## 3. 플랫폼 등록

### 3.1 Web 플랫폼 추가
1. 좌측 메뉴에서 **앱 설정** > **플랫폼** 클릭
2. **Web 플랫폼 등록** 버튼 클릭
3. 사이트 도메인 등록:
   - **개발 환경**: `http://localhost:3000`
   - **운영 환경**: `https://yourdomain.com` (실제 도메인)
4. **저장** 클릭

### 3.2 모바일 앱 등록 (선택사항)
- iOS/Android 앱이 있다면 추가로 등록

## 4. 환경변수 설정

### 4.1 `.env.local` 파일 생성
프로젝트 루트(`/web/` 디렉토리)에 `.env.local` 파일을 생성하고 다음 내용 추가:

```bash
# Supabase (기존)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Kakao
NEXT_PUBLIC_KAKAO_APP_KEY=여기에_JavaScript_키_입력
```

**예시:**
```bash
NEXT_PUBLIC_KAKAO_APP_KEY=1234567890abcdef1234567890abcdef
```

### 4.2 개발 서버 재시작
환경변수를 추가했다면 개발 서버를 재시작해야 합니다:

```bash
cd web
npm run dev
```

## 5. 카카오톡 공유하기 이미지 설정 (선택사항)

### 5.1 OG 이미지 생성
카카오톡에서 보기 좋은 공유 이미지를 만들어야 합니다:

- **권장 크기**: 1200 x 630px
- **파일 형식**: PNG 또는 JPEG
- **내용**: 앱 로고, 타이틀, 간단한 설명

### 5.2 이미지 업로드
1. `/web/public/` 디렉토리에 `climate-mbti-og.png` 파일 저장
2. 또는 외부 이미지 호스팅 사용 (Cloudinary, Imgur 등)

### 5.3 코드에서 이미지 URL 변경
`/web/app/climate-test/result/page.tsx` 파일에서:

```typescript
imageUrl: `${window.location.origin}/climate-mbti-og.png`,
```

외부 URL 사용 시:
```typescript
imageUrl: 'https://your-cdn.com/climate-mbti-og.png',
```

## 6. 동작 확인

### 6.1 테스트 환경
1. 개발 서버 실행: `npm run dev`
2. 테스트 완료 후 결과 페이지에서 **카카오톡 공유** 버튼 클릭
3. 카카오톡 공유 팝업이 뜨는지 확인

### 6.2 문제 해결

#### "NEXT_PUBLIC_KAKAO_APP_KEY가 설정되지 않았습니다" 에러
- `.env.local` 파일에 키를 제대로 입력했는지 확인
- 개발 서버를 재시작했는지 확인
- 환경변수 이름이 정확한지 확인 (`NEXT_PUBLIC_` 접두사 필수)

#### "Kakao SDK not loaded yet" 경고
- 페이지가 완전히 로드되기 전에 공유 버튼을 클릭한 경우
- 잠시 후 다시 시도

#### 공유 팝업이 뜨지 않음
- 브라우저 팝업 차단 설정 확인
- 개발자 도구 콘솔에서 에러 메시지 확인
- 카카오 개발자 센터에서 도메인이 제대로 등록되었는지 확인

## 7. 배포 환경 설정

### 7.1 Vercel 환경변수 설정
1. Vercel 프로젝트 대시보드 접속
2. **Settings** > **Environment Variables** 클릭
3. 다음 환경변수 추가:
   - Key: `NEXT_PUBLIC_KAKAO_APP_KEY`
   - Value: 카카오 JavaScript 키
   - Environment: Production, Preview, Development 모두 선택
4. **Save** 클릭
5. 프로젝트 재배포

### 7.2 도메인 추가
카카오 개발자 센터에서 실제 운영 도메인 추가:
1. **앱 설정** > **플랫폼**
2. 운영 도메인 추가 (예: `https://gasout.vercel.app`)

## 8. 추가 기능 (선택사항)

### 8.1 카카오 스토리 공유
`shareToKakaoStory` 함수를 사용하여 카카오 스토리에도 공유 가능

### 8.2 공유 성공 추적
GA4나 Mixpanel로 공유 버튼 클릭 추적:

```typescript
// 공유 성공 시
gtag('event', 'share', {
  method: 'kakao',
  content_type: 'climate_test_result',
  item_id: result.type
});
```

## 9. 참고 자료

- [카카오 디벨로퍼스 - 카카오톡 공유](https://developers.kakao.com/docs/latest/ko/message/js-link)
- [카카오 JavaScript SDK 가이드](https://developers.kakao.com/docs/latest/ko/sdk-download/js)
- [Next.js 환경변수 가이드](https://nextjs.org/docs/app/building-your-application/configuring/environment-variables)

## 10. 보안 주의사항

⚠️ **JavaScript 키는 공개되어도 안전**하지만, **Admin 키는 절대 노출하면 안 됩니다!**

- ✅ JavaScript 키 → `.env.local`에 저장 (Git에 커밋 안 됨)
- ❌ Admin 키 → 절대 프론트엔드에서 사용 금지
- ✅ `.env.local`은 `.gitignore`에 포함되어 있는지 확인

---

설정 완료 후 카카오톡 공유가 정상 작동합니다! 🎉

